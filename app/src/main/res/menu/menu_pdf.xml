<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_search"
        android:contentDescription="@string/search"
        android:icon="@drawable/ic_search"
        android:title="@string/search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_show_as_list"
        android:contentDescription="@string/show_as_list"
        android:icon="@drawable/ic_list_menu"
        android:title="@string/show_as_list"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_scroll_up"
        android:contentDescription="@string/scroll_up"
        android:icon="@drawable/ic_arrow_up"
        android:title="@string/scroll_up"
        app:showAsAction="ifRoom" />
</menu>